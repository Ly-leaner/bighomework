#ifndef STRATWINDOW_H
#define STRATWINDOW_H

#include <QMainWindow>

class StratWindow : public QMainWindow
{
    Q_OBJECT
public:
    explicit StratWindow(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // STRATWINDOW_H